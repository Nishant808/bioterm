# Runtime deps for the Streamlit Community Cloud dashboard.
# (The dashboard adds ./src to sys.path, so the `bioterm` package itself is not
#  installed here — only its third-party dependencies. Keep in sync with pyproject.toml.)
# rebuild-marker: 2026-09-10 portfolio.2  (bump to force a full Streamlit Cloud rebuild
# when only .py files changed — otherwise stale helper modules stay in sys.modules)
sqlalchemy>=2.0
psycopg[binary]>=3.1        # Postgres driver for the cloud DATABASE_URL
yfinance>=0.2.40
pandas>=2.1
numpy>=1.26
requests>=2.31
feedparser>=6.0
beautifulsoup4>=4.12
lxml>=5.0
vaderSentiment>=3.3.2
apscheduler>=3.10
streamlit>=1.36
plotly>=5.22
pyyaml>=6.0
typer>=0.12
rich>=13.0
python-dateutil>=2.9
openpyxl>=3.1
tenacity>=8.3
