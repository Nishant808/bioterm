# Runtime deps for the Streamlit Community Cloud dashboard.
# (The dashboard adds ./src to sys.path, so the `bioterm` package itself is not
#  installed here — only its third-party dependencies. Keep in sync with pyproject.toml.)
# rebuild-marker: 2026-09-25 terminal-v4
# NB: a comment-only edit here does NOT reliably make Streamlit Cloud re-run the
# install — it keeps imported modules (bioterm.*, _shared, _ui) in sys.modules on a
# fast reboot. When a page needs a *new* symbol from a long-lived module, change a
# real dependency line below (that forces a clean rebuild) or reboot from the
# Streamlit Cloud console.
sqlalchemy>=2.0
watchdog>=6.0               # faster + more reliable file-watch on Streamlit Cloud (floor bumped
                            # from 4.0 to force a clean Cloud rebuild - session 7)
psycopg[binary]>=3.1        # Postgres driver for the cloud DATABASE_URL
yfinance>=0.2.40
pandas>=2.2               # floor bumped from 2.1 to force a clean Cloud rebuild (session 9:
                          # pages import new bioterm.* symbols - see CLAUDE.md deploy gotcha)
numpy>=1.26
requests>=2.32              # floor bumped from 2.31 to force a clean Cloud rebuild (session 7)
feedparser>=6.0
beautifulsoup4>=4.12
lxml>=5.0
vaderSentiment>=3.3.2
apscheduler>=3.10
streamlit>=1.63        # bumped from 1.36 - also forces Streamlit Cloud's fast-reboot
                       # cache of already-imported modules (_ui, _shared, bioterm.*)
                       # to actually clear; see CLAUDE.md's deploy gotcha
plotly>=6.0            # bumped from 5.22 (session 7 UI release) - a real
                       # dependency change forces the clean Cloud rebuild
pyyaml>=6.0
typer>=0.12
rich>=13.0
python-dateutil>=2.9
openpyxl>=3.1
tenacity>=8.3
# session 11 (terminal release) - new runtime deps; adding them is also the real
# dependency change that makes Streamlit Cloud do the clean rebuild this release needs
cryptography>=42            # the encrypted key vault (Settings -> API keys)
anthropic>=1.8              # Copilot, AI event extraction, filing summaries, daily brief
Authlib>=1.6                # optional owner sign-in via the app's OIDC provider
